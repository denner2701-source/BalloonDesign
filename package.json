{
  "name": "balloon-design",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:vendor": "esbuild vendor-entry.js --bundle --minify --format=iife --platform=browser --target=es2020 --outfile=vendor.js",
    "test": "node --test tests/*.test.cjs",
    "check": "npm test"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.111.0",
    "three": "0.185.1"
  },
  "devDependencies": {
    "esbuild": "0.25.9"
  }
}
